import React from 'react'

const UserContext = React.createContext()

export default UserContext;

//This is the first step in creating a user context. In the next step we need to create a provider too
//This file is '.js' because it is purely a javascript file